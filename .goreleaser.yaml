# .goreleaser.yaml
# Production release configuration for URL Shortener using GoReleaser v2
version: 2

project_name: url-shortener

before:
  hooks:
    - go mod tidy

builds:
  - id: url-shortener
    main: ./server/cmd/api
    binary: url-shortener
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -extldflags '-static'
      - -X github.com/semmidev/url-shortener/server/internal/config.Version={{.Version}}
      - -X github.com/semmidev/url-shortener/server/internal/config.BuildTime={{.Date}}
      - -X github.com/semmidev/url-shortener/server/internal/config.GitCommit={{.Commit}}

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - compose.yml
      - .env.example

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "merge conflict"
      - Merge pull request
      - Merge branch
  groups:
    - title: "🚀 Features"
      regexp: '^feat[(\w\s)]*:'
      order: 0
    - title: "🐛 Bug Fixes"
      regexp: '^fix[(\w\s)]*:'
      order: 1
    - title: "⚡ Performance & Refactoring"
      regexp: '^(perf|refactor)[(\w\s)]*:'
      order: 2
    - title: "🛠️ Maintenance & Infrastructure"
      regexp: '^(build|ci|chore)[(\w\s)]*:'
      order: 3
    - title: "📋 Documentation"
      regexp: '^docs[(\w\s)]*:'
      order: 4
    - title: "📦 Others"
      order: 999

release:
  github:
    owner: semmidev
    name: url-shortener
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## 📦 URL Shortener Release {{ .Version }} ({{ .Date }})

    High-performance URL Shortener API release binary package with embedded React SPA.

  footer: |
    ---
    *Automated release generated by [GoReleaser](https://goreleaser.com) & GitHub Actions.*
