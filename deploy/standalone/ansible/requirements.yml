---
# ansible/requirements.yml
# Install dengan: ansible-galaxy collection install -r requirements.yml

collections:
  - name: community.general
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
